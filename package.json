{
  "name": "aviasales-clone",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "app": "npm run dev -w packages/app",
    "api": "npm run dev -w packages/api",
    "dev": "npm-run-all --parallel app api"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
